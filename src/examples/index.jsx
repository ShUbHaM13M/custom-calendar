import { TextInput } from '../components';

const Examples = () => {
  return (
    <div className='flex flex-col'>
      <TextInput label='Label' name='label' placeholder='Input' required />
    </div>
  );
};

export default Examples;
